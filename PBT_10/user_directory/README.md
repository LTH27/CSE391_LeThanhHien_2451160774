# User Directory

API: JSONPlaceholder

Cách chạy:
1. Mở file `index.html` trong trình duyệt.
2. Tải user, tìm kiếm, thêm, sửa, xóa.

Chức năng:
- READ/CREATE/UPDATE/DELETE
- Search client-side
- Loading state và lỗi hiển thị
