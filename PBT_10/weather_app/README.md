# Weather App

API: Open-Meteo (geocoding + current weather)

Cách chạy:
1. Mở file `index.html` trong trình duyệt.
2. Nhập tên thành phố và bấm Tìm.

Chức năng:
- Loading / Success / Error state
- Lưu lịch sử tìm kiếm vào LocalStorage
- Click lịch sử để tìm lại
